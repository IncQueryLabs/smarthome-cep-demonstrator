package homeioexample


rule "E Lights Motion"
    when
        $light : Item( name == "E_Lights_Analog", $lightState : state)
        Item( name == "E_Brightness_Sensor", $brightnessSensorState: state)

        ItemStateChangedEvent( name == "E_Motion_Detector", $motionDetectorState : newState)
    then
        if ($lightState.equals(ZERO) && $motionDetectorState == MOTION && $brightnessSensorState == DARKNESS) {
            openhab.postCommand($light, ON);
        }
end