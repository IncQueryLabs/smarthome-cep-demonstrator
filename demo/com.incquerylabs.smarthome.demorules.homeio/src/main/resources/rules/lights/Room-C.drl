package homeioexample


rule "C lights"
    when
        $item : Item( name == "C_Lights", $state : state)

        $itemState : ItemStateChangedEvent( name == "C_Light_Switch", newState == OPEN)
    then
        if ($state == ON) {
            openhab.postCommand($item, OFF);
        } else {
            openhab.postCommand($item, ON);
        }
end